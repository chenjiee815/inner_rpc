from distutils.core import setup

setup(
    name="inner_rpc",
    version="1.0.0.0",
    author="Jie Chen",
    author_email="chenjiee815@gmail.com",
    description=u"RPC which use unix domain socket to connect with each others",
    packages=["inner_rpc"],
)
